import './AboutProject.css';
import BlockTitle from "../BlockTitle/BlockTitle";

function AboutProject() {
    return (
        <section className="about">
            <BlockTitle titleText={'kek'}></BlockTitle>
        </section>
    );
}

export default AboutProject;
